version: "3.9"

services:
  population-api:
    build: .
    container_name: population-api
    ports:
      - "8000:8000"    # FastAPI — population + pie SVG charts
    volumes:
      - db-data:/app    # Persist SQLite database
    restart: unless-stopped

volumes:
  db-data:
